import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 10,
    padding: 10,
    paddingTop: 30,
    paddingBottom: 100,
  },
  message: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#000000',
    alignSelf: 'center',
    paddingVertical: 50,
  },
});
