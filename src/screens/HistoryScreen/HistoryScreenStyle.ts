import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    gap: 10,
    padding: 10,
    paddingTop: 30,
    paddingBottom: 100,
  },
  text: {
    color: '#000000',
    paddingVertical: 5,
    textAlign: 'justify',
  },
});
