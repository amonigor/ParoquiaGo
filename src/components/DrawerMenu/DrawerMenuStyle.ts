import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: '100%',
  },
  image: {
    marginBottom: 30,
    alignSelf: 'center',
  },
  text: {
    bottom: 0,
    padding: 10,
    fontSize: 10,
    position: 'absolute',
    color: '#000000',
  },
});
